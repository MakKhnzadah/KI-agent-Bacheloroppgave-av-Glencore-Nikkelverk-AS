# Local-first config (no Azure/cloud required).
#
# This MVP uses a heuristic suggestion generator and does not require any API keys.
# Keep this placeholder for future iterations.
OPENAI_API_KEY=

# Paths
KB_RAW_DIR=databases/knowledge_base/raw
KB_HTML_DIR=databases/knowledge_base/html
DATA_DIR=databases/data
