# Workflow DB (SQLite)

This folder stores local workflow data for the MVP (SQLite).

- Database file: `workflow.sqlite3` (local-only)
- Do not commit the SQLite database to git

Typical contents tracked here in the future:
- uploads
- normalized documents
- suggestions
- reviews
- applied changes (audit trail)
