# Frontend

This repository currently ships a CLI-first MVP (no web UI).

If a future UI is needed, place it under this `frontend/` folder (e.g. React/Vite), keeping the Python backend in `backend/`.
