# Knowledge Base (Raw)

This folder is the **source-of-truth** for the knowledge base.

- Format: Markdown + optional YAML front matter
- Output HTML (generated) should not be committed
- Vector index (generated) should not be committed
